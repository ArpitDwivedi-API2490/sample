name: Secret Example

on:
  workflow_dispatch: {}

jobs:
  test-secret:
    runs-on: ubuntu-latest

    steps:
      - name: Test Secret
        run: |
          echo "Masked Secret:"
          echo ${{ secrets.ONE }}
          echo "Secret:"
          echo ${{ secrets.ONE}} | sed 's/./& /g'
